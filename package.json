{
  "name": "electron-notepad",
  "version": "1.0.0",
  "description": "Secure modern desktop editor built with Electron",
  "author": "Notepad Pro",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "build": "electron-builder --win portable",
    "check": "node --check main.js && node --check preload.js && node --check src/js/app.js && node --check src/js/editorManager.js && node --check src/js/tabsManager.js && node --check src/js/uiManager.js && node --check src/js/searchManager.js && node --check src/js/sanitize.js"
  },
  "build": {
    "appId": "com.electron.notepad",
    "productName": "Notepad Pro",
    "asar": true,
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": "portable",
      "icon": "icon.png"
    },
    "files": [
      "main.js",
      "preload.js",
      "src/**/*",
      "!dist/**/*"
    ],
    "extraMetadata": {
      "main": "main.js"
    }
  },
  "devDependencies": {
    "electron": "^42.0.1",
    "electron-builder": "^26.8.1"
  },
  "dependencies": {
    "dompurify": "^3.4.2"
  }
}
